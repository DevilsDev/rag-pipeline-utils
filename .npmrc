# NPM configuration for supply-chain security
registry=https://registry.npmjs.org/
save-exact=true
package-lock=true
audit-level=moderate
fund=false

# Use environment variables for authentication tokens
# Example: NPM_TOKEN=your_token_here